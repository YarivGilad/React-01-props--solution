export default function create_message(available) {
    return available === true ? 'Going on vacations' : 'Too busy for vacations...'
}


// export default function (available) {
// 	return available === true ? 'Going on vacation' : 'Too busy for vacations...'
// }


// export const create_message = (available) => {
// 	return available === true ? 'Going on vacations' : 'Too busy for vacations...'
// }

// export default function(available) {
// 	return available === true ? (
// 		<h1>Going on vacation</h1>
// 	) : (
// 		<h2>Too busy for vacations...</h2>
// 	)
// }
